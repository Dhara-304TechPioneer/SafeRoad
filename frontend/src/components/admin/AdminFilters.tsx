import type { AdminFilters as Filters } from '../../types/admin';
interface Props { filters: Filters; departments: string[]; onChange: (value: Filters) => void; onReset: () => void; }
export const AdminFilters = ({ filters, departments, onChange, onReset }: Props) => <section className="admin-filters" aria-label="Admin portal filters"><label>Search<input value={filters.search} onChange={(event) => onChange({ ...filters, search: event.target.value })} placeholder="Search all entities" /></label><label>Department<select value={filters.department} onChange={(event) => onChange({ ...filters, department: event.target.value })}><option>All</option>{departments.map((item) => <option key={item}>{item}</option>)}</select></label><label>Role<select value={filters.role} onChange={(event) => onChange({ ...filters, role: event.target.value })}><option>All</option><option>Administrator</option><option>Officer</option><option>Analyst</option><option>Citizen</option></select></label><label>Status<select value={filters.status} onChange={(event) => onChange({ ...filters, status: event.target.value })}><option>All</option><option>Active</option><option>Suspended</option><option>New</option><option>Assigned</option><option>Resolved</option><option>On duty</option></select></label><label>Date<select value={filters.date} onChange={(event) => onChange({ ...filters, date: event.target.value })}><option>All</option><option>7 days</option><option>30 days</option></select></label><button type="button" onClick={onReset}>Reset filters</button></section>;
