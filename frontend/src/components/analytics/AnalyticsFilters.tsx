import type { AnalyticsFilters as Filters } from '../../types/analytics';
interface Props { filters: Filters; options: Record<string, string[]>; onChange: (filters: Filters) => void; onReset: () => void; }
const fields: { key: keyof Filters; label: string }[] = [{ key: 'city', label: 'City' }, { key: 'severity', label: 'Severity' }, { key: 'status', label: 'Status' }, { key: 'dateRange', label: 'Date range' }, { key: 'department', label: 'Department' }, { key: 'reporter', label: 'Reporter' }, { key: 'vehicleType', label: 'Vehicle type' }];
export const AnalyticsFilters = ({ filters, options, onChange, onReset }: Props) => <section className="analytics-filters" aria-label="Analytics filters">{fields.map(({ key, label }) => <label key={key}>{label}<select value={filters[key]} onChange={(event) => onChange({ ...filters, [key]: event.target.value })}><option>All</option>{(options[key] ?? []).map((option) => <option key={option}>{option}</option>)}</select></label>)}<button type="button" onClick={onReset}>Reset filters</button></section>;
