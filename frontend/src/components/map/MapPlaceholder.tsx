import type { MapReport } from '../../types/map';
import { MapMarker } from './MapMarker';
interface MapPlaceholderProps { reports: MapReport[]; selectedReport: MapReport | null; zoom: number; heatmap: boolean; onSelect: (report: MapReport) => void; }
export const MapPlaceholder = ({ reports, selectedReport, zoom, heatmap, onSelect }: MapPlaceholderProps) => <section className={`map-placeholder map-placeholder--zoom-${zoom} ${heatmap ? 'map-placeholder--heatmap' : ''}`} aria-label="Interactive road safety map placeholder"><div className="map-placeholder__roads" /><div className="map-placeholder__water" /><div className="map-placeholder__heat" /><div className="map-placeholder__intro"><strong>Interactive Road Safety Map</strong><span>Leaflet integration coming soon</span></div><div className="map-placeholder__markers">{reports.map((report) => <MapMarker key={report.id} report={report} index={Number(report.id.slice(-4)) - 1} isSelected={selectedReport?.id === report.id} onSelect={onSelect} />)}</div></section>;
