import { FiDownload, FiRefreshCw, FiSearch } from 'react-icons/fi';
import type { MapSort } from '../../types/map';
interface MapToolbarProps { search: string; reportCount: number; sort: MapSort; isRefreshing: boolean; onSearchChange: (value: string) => void; onSortChange: (value: MapSort) => void; onRefresh: () => void; onExport: () => void; }
export const MapToolbar = ({ search, reportCount, sort, isRefreshing, onSearchChange, onSortChange, onRefresh, onExport }: MapToolbarProps) => <header className="map-toolbar"><div className="map-toolbar__heading"><span className="map-toolbar__eyebrow">OPERATIONS OVERVIEW</span><h1>Live Road Safety Map</h1></div><label className="map-toolbar__search" htmlFor="map-search"><FiSearch aria-hidden="true" /><input id="map-search" value={search} onChange={(event) => onSearchChange(event.target.value)} placeholder="Search city, reporter, title or ID" /></label><div className="map-toolbar__meta"><strong>{reportCount}</strong><span>reports shown</span></div><label className="map-toolbar__sort">Sort<select value={sort} onChange={(event) => onSortChange(event.target.value as MapSort)}><option>Newest</option><option>Oldest</option><option>Highest Severity</option><option>Most Reports</option><option>Alphabetical</option></select></label><span className="map-toolbar__updated">Updated just now</span><button className="button-secondary" type="button" disabled={isRefreshing} onClick={onRefresh}><FiRefreshCw className={isRefreshing ? 'spin-animation' : ''} />Refresh</button><button className="button-secondary map-toolbar__export" type="button" aria-disabled="true" onClick={onExport}><FiDownload />Export</button></header>;
