// Local-only comment interface that is ready to connect to a discussion API.
import { useState, type FormEvent } from 'react';
import { reportComments } from '../../data/reportManagementData';
import { addComment } from '../../services/reportManagementService';
export const CommentSection = () => { const [comment, setComment] = useState(''); const [comments, setComments] = useState(reportComments); const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!comment.trim()) return; await addComment(comment); setComments((current) => [...current, { author: 'Ananya Patel', role: 'Citizen', message: comment, timestamp: 'Just now', initials: 'AP' }]); setComment(''); }; return <section className="detail-section"><h2>Comments</h2>{comments.map((item) => <article className="comment" key={`${item.author}-${item.timestamp}`}><span>{item.initials}</span><div><strong>{item.author} <small>{item.role}</small></strong><p>{item.message}</p><time>{item.timestamp}</time></div></article>)}<form className="comment-form" onSubmit={submit}><label htmlFor="comment">Add a comment</label><textarea id="comment" value={comment} onChange={(event) => setComment(event.target.value)} placeholder="Share an update or question" /><button className="button-primary">Send</button></form></section>; };
