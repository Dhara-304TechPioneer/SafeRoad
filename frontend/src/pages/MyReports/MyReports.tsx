// Citizen report catalogue with frontend-only filters and view controls.
import { useMemo, useState } from 'react';
import { managedReports } from '../../data/reportManagementData';
import { ReportCard } from '../../components/reportManagement/ReportCard';
import { ReportStatistics } from '../../components/reportManagement/ReportStatistics';
import './MyReports.css';
export const MyReports = () => { const [query, setQuery] = useState(''); const [status, setStatus] = useState('All'); const [severity, setSeverity] = useState('All'); const [sort, setSort] = useState('newest'); const [grid, setGrid] = useState(true); const reports = useMemo(() => managedReports.filter((report) => (status === 'All' || report.status === status) && (severity === 'All' || report.severity === severity) && `${report.id} ${report.location}`.toLowerCase().includes(query.toLowerCase())).sort((a, b) => sort === 'newest' ? b.id.localeCompare(a.id) : a.id.localeCompare(b.id)), [query, severity, sort, status]); return <main className="my-reports"><header><p className="eyebrow">CITIZEN REPORTING</p><h1>My Reports</h1><p>Track each road-safety report from submission to completion.</p></header><ReportStatistics reports={managedReports} /><section className="report-filter-bar"><input aria-label="Search reports" placeholder="Search report ID or location" value={query} onChange={(event) => setQuery(event.target.value)} /><select value={status} onChange={(event) => setStatus(event.target.value)}><option>All</option>{['Reported', 'AI Verified', 'Officer Verified', 'Repair Assigned', 'Under Repair', 'Completed'].map((value) => <option key={value}>{value}</option>)}</select><select value={severity} onChange={(event) => setSeverity(event.target.value)}><option>All</option>{['Low', 'Medium', 'High', 'Critical'].map((value) => <option key={value}>{value}</option>)}</select><select value={sort} onChange={(event) => setSort(event.target.value)}><option value="newest">Newest first</option><option value="oldest">Oldest first</option></select><button className="button-secondary" onClick={() => setGrid((value) => !value)}>{grid ? 'List view' : 'Grid view'}</button></section><div className={grid ? 'reports-grid' : 'reports-list'}>{reports.slice(0, 8).map((report) => <ReportCard key={report.id} report={report} />)}</div><nav className="pagination" aria-label="Report pagination"><button className="button-secondary">Previous</button><strong>1</strong><button className="button-secondary">2</button><button className="button-secondary">Next</button></nav></main>; };
