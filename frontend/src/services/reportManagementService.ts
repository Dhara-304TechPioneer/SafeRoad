// Async placeholders for future report management API integration.
export const getMyReports = () => Promise.resolve();
export const getReportById = (reportId: string) => { void reportId; return Promise.resolve(); };
export const addComment = (message: string) => { void message; return Promise.resolve(); };
